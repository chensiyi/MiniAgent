{
  "name": "miniagent",
  "version": "0.2.8",
  "private": true,
  "type": "module",
  "description": "MiniAgent —— 极简 userScript 智能体（原生 DOM + GM_xmlhttpRequest 桥接 LLM，无框架）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vite build --mode test && vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.0",
    "vite-plugin-monkey": "^8.0.6"
  }
}
